# Lectura y proyeccion de Parquet en lotes (RecordBatchReader).
# Piso en 1.1: `to_arrow_reader` no existe en las series 0.x.
duckdb>=1.1.0
# Motor de dataframes para la validacion de calidad vectorizada.
pandas>=2.0.0
numpy>=1.24.0
# Formato columnar en memoria que conecta DuckDB con pandas sin copias.
pyarrow>=12.0.0
# Driver PostgreSQL (binario: no requiere compilar libpq en la maquina).
psycopg2-binary>=2.9.0
# API REST y servidor WSGI de produccion.
flask>=2.3.0
gunicorn>=21.2.0
# Descarga HTTP en streaming de los Parquet de la TLC.
requests>=2.28.0
# Carga de credenciales desde .env (nada hardcodeado en el codigo).
python-dotenv>=1.0.0
# Pruebas automatizadas.
pytest>=7.4.0
